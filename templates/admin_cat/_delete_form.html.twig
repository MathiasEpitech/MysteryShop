
	<form class="text-center" method="post" action="{{ path('app_admin_cat_delete', {'id': categorie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
		<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie.id) }}">
		<button class="btn btn-sm bg-danger text-white m-1 p-1"><i class="fa-solid fa-trash"></i></button>
	</form>

