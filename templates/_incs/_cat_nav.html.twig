


<li class="nav-item">
	<a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="{{ path("app_produit") }}" type="button" aria-haspopup="true" aria-expanded="false"></a>
	<div class="dropdown-menu dropdown-menu-start">

		<a href="{{ path("app_categories") }}" class="dropdown-item">---Toutes les categories---</a>
        {% for cat in categories_g.findAll() %}
            
        
		<a class="dropdown-item" href="{{ path("app_categorie_produit", {'id' : cat.id}) }}">{{cat.nom}}</a>

        {% endfor %}
	</div>
</li>
