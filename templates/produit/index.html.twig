{% extends 'base.html.twig' %}

{% block title %}Produits
{% endblock %}

{% block body %}


	<div class="container py-4 justify-content-center">

		<h2 class="text-center text-white bg-dark border border-success col-md-7 col-lg-7 py-3 mx-auto my-2 rounded">PRODUITS</h2>

		<div class=" py-5">

			<div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-3">

				{% for produit in produits %}

					

					<div class="">

                        <div class="container" id="container">

						    <img src="{{ asset('uploads/images/')}}{{ produit.image }}" alt="Avatar" id="image" class="image bg-light" style="width:107%; margin-bottom: 50px;">

						    <div class="middle">
                            

                                <a class="" href="{{ path('app_detail_produit', { 'id': produit.id} ) }}">
							    <h1 class="text" id="">{{ produit.nom }}</h1>

                                <p class="text-center fs-2 fw-bold">{{ produit.prix}}€</p>

                                </a>
						    </div>
					    </div>

						{# {{ <div class="card shadow-sm">

							<img src="{{ asset('uploads/images/')}}{{ produit.image }}" class="img-fluid p-1"/>
							<img src="{{ produit.image}}" class="img-fluid p-1">

							<h1 class="title text-center p-1">
								{{ produit.nom }}
							</h1>

							<div class="card-body">

								<p class="text-center fs-2 fw-bold">{{ produit.prix}}€</p>

								<div class="d-flex justify-content-between align-items-center">

									<div class="d-block mx-auto">

										<a class="btn btn-lg btn-outline-secondary m-1" href="{{ path('app_detail_produit', { 'id': produit.id} ) }}">
											<i class="fa-solid fa-eye"></i>
										</a>
										<a class="btn btn-lg btn-outline-success m-1" href="{{ path("app_panier_ajouter", { 'id': produit.id} ) }}">
											<i class="fa-solid fa-cart-plus"></i>
										</a>

									</div>

								</div>

							</div>

						</div> | replace("search", "replace") }} #}

					</div>
				{% endfor %}

			</div>

		</div>

	</div>

{% endblock %}
